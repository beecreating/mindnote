export const theme = {
  paper: "#FAF7F0",        // warm off-white
  ink: "#111111",          // near-black
  gridBlue: "#BFD7EA",     // faint blue grid
  marginRed: "#D99A9A",    // faint red margin
  rule: "rgba(0,0,0,0.10)" // thin divider lines
} as const;

export const typography = {
  size: {
    sm: 12,
    md: 14,
    lg: 16,
    xl: 18,
  },
  weight: {
    regular: "400",
    medium: "500",
    semibold: "600",
  },
} as const;


